#include "optimization.h"
#include <iostream>
#include <cstdint>
#include <unordered_map>
#include <vector>
#include <string>



int main() {
	int hh = readInt();
    // readChar();
    int mm = readInt(), a = readInt(), b = readInt(), c = readInt(), d = readInt();
    
    writeInt(hh, ' ');
    writeInt(mm, ' ');
    writeInt(a, ' ');
    writeInt(b, ' ');
    writeInt(c, ' ');
    writeInt(d, '\n');


    return 0;
}